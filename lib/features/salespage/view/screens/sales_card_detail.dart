import 'package:flutter/material.dart';

class SalesCardDetails extends StatelessWidget {
  const SalesCardDetails({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}